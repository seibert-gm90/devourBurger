<h1>Burgers!</h1>

<h2>Burgers available to eat</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burgers-block eaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that have been eaten</h2>

<ul>
  {{#each burgers}}
    {{#if eaten}}
      {{> burgers/burgers-block eaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Eat a burger!</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">This burger has been devoured?</label><br>
    <input type="radio" name="eaten" value="1" checked> Devoured!<br>
    <input type="radio" name="eaten" value="0"> Available to eat
  </div>

  <button type="submit">Eat Burger</button>
</form>
